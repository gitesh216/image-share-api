services:
  mongodb:
    image: mongo:7
    container_name: imageshare-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: changeme
      MONGO_INITDB_DATABASE: imageshare
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: